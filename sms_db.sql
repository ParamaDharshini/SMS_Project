CREATE TABLE students (
    roll_no INT PRIMARY KEY,
    name VARCHAR(100),
    gender VARCHAR(10),
    percentage DOUBLE,
    grade VARCHAR(5),
    email VARCHAR(100),
    phone_number VARCHAR(15),
    course VARCHAR(100)
);
